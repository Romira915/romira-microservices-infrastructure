---
# tasks file for roles/bitwarden
- import_tasks: unix_bitwarden.yml
  when: ansible_facts['ansible_system'] in ['Linux', 'Darwin']

- import_tasks: win_bitwarden.yml
  when: ansible_facts['ansible_system'] == 'Windows'
